@model IEnumerable<Filial>

@{
    ViewData["Title"] = "Filiais";
}


<div class="pai-filial">
    <div class="filho btable-hover table-secondary col-10 ">
        <h2 class="font text-center m-2 font-weight-bold">Filiais</h2>
        <hr />
        <table class="table table-dark rounded font font-we btable-hover table-secondary table-responsive-md table-responsive-sm col-md-12 table-responsive-lg">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PessoaJuridica.Nome)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PessoaJuridica.CNPJ)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PessoaJuridica.NumeroMunicipal)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NumeroRegistroNaJuntaComercial)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Empresa.Nome)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Ativo)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.PessoaJuridica.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PessoaJuridica.CNPJ)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PessoaJuridica.NumeroMunicipal)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NumeroRegistroNaJuntaComercial)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Empresa.Nome)
                        </td>
                        <td>
                            @{ if (item.Ativo == false)
                                {
                                    <i class="material-icons text-danger">close</i>

                                }
                                else
                                {
                                    <i class="material-icons text-center text-success">check</i>

                                }
                            }
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-primary rounded-pill font m-1 float-right">Detalhes</a>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-success rounded-pill font m-1 float-right">Editar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <p class="text-center bt-b">
            <a asp-action="Create" class="btn btn-primary btns rounded-pill text-decoration-none add"><i class="material-icons float-left ">add</i>Adicionar</a>
        </p>
    </div>
</div>